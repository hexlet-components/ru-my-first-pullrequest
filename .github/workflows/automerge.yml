name: automerge
on:
  pull_request_target:
    types:
      - labeled
      - unlabeled
      - synchronize
      - opened
      - edited
      - ready_for_review
      - reopened
      - unlocked
  pull_request_review:
    types:
      - submitted
  check_suite:
    types:
      - completed
  status: {}
jobs:
  automerge:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{github.event.pull_request.head.ref}}
          repository: ${{github.event.pull_request.head.repo.full_name}}
          fetch-depth: 2
      - name: Checking that workflow file did not change
        run: git diff HEAD HEAD~1 --exit-code .github/workflows/automerge.yml
      - name: automerge
        uses: "pascalgn/automerge-action@v0.16.4"
        env:
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
          MERGE_LABELS: ""
          MERGE_DELETE_BRANCH: "true"
          MERGE_FORKS: "true"




local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))() local Window = Library.CreateLib("Myrax", "BloodTheme") 
--ESP local Esp = Window:NewTab("Esp") local EspSection = Esp:NewSection("Esp") 

EspSection:NewButton("Esp", "Esp", function() loadstring(game:HttpGet("https://raw.githubusercontent.com/Ihaveash0rtnamefordiscord/Releases/main/MurderMystery2HighlightESP"))(' Watermelon ?') end) 
--HITBOX local Hitbox = Window:NewTab("Hitbox") local HitboxSection = Hitbox:NewSection("Hitbox") 

HitboxSection:NewButton("Hitbox", "Hitbox", function() loadstring(game:HttpGet("https://raw.githubusercontent.com/tbao143/thaibao/main/TbaoHubMurdervssheriff"))() end) 
--AIM local aim = Window:NewTab("Aim") local AimSection = Aim:NewSection("Aim") 

AimSection:NewButton("Silentaim(key)", "Aimbot", function() loadstring(game:HttpGet("https://raw.githubusercontent.com/script-by/Nx-Crazy/main/Silent-Aim-MM2"))() end) 
AimSection:NewButton("Silentaim(keyless)", "Aimbot", function() loadstring(game:HttpGet("https://raw.githubusercontent.com/Joystickplays/psychic-octo-invention/main/yarhm.lua", false))() end)
